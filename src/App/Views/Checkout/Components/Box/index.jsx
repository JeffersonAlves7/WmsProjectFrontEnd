import api from '../../../../api/index'
import { useState, useeffect } from 'react'

export default (props) => {
    const { itens } = props //Precisa ser um array
    console.log(itens)
    return (
        <section id='area-items'>

        </section>
    )
}
